<div>
  <div>

    <div>
      <h2>ショッピングカート</h2>
      <%= link_to "カートを空にする", cart_items_destroy_all_path, method: :delete, class:"btn btn-danger" %>
    </div>

    <div>
      <div>
        <table class="table">
          <thead>
            <tr>
              <th>商品名</th>
              <th>単価（税込）</th>
              <th>数量</th>
              <th>小計</th>
              <th></th>
            </tr>
          </thead>
        <% @cart_items.each do |cart_item| %>
          <tbody>
            <td><%= image_tag cart_item.get_image %><%= cart_item.item.name %></td>
            <td><%= number_to_currency( cart_item.item.with_tax_price, precision: 0, unit: "") %></td>
            <td>
              <%= form_with model: cart_item, url: cart_items_create_path, local:true do |f| %>
              <%= f.select :amount, *[1..5] %>
              <%= f.submit "変更", class:"btn btn-success" %>
              <% end %>
            </td>
            <td><%= number_to_currency( cart_item.subtotal, precision: 0, unit: "") %></td>
            <td><%= link_to "削除する", cart_items_destroy_path(cart_item), method: :delete, class:"btn btn-danger" %></td>
          </tbody>
        </table>
        <table>
          <tbody>
            <tr>
              <td>合計金額</td>
              <td><%= number_to_currency( @total_price += order_detail.subtotal, precision: 0, unit: "") %></td>
            </tr>
          </tbody>
        </table>
        <% end %>
      </div>
      <div>
        <%= link_to "買い物を続ける", root_path, class:"btn btn-primary" %>
        <%= link_to "情報入力に進む", new_order_path, class:"btn btn-success" %>
      </div>
    </div>

  </div>
</div>
